# Copy this file to .env.local and fill in your secrets.
# Vite only exposes variables prefixed with VITE_

VITE_GEMINI_API_KEY=
# Optional: override model if you like
VITE_GEMINI_MODEL=gemini-2.5-flash
